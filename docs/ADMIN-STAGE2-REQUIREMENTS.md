# luoji-admin · 阶段二开发文档（需求 + 技术选型）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0（草案，待确认） |
| 撰写日期 | 2026-09-11 |
| 上游文档 | [`ADMIN-REQUIREMENTS.md`](./ADMIN-REQUIREMENTS.md) v1.1（阶段一已交付） |
| 前置状态 | 阶段一 ✅：展示站点 + 后台管理双区域、RLS 收紧、7 天会话有效期均已上线 |
| 本阶段目标 | 把「能发内容」升级为「**好发内容、发得安全、可运营**」 |

> 说明：本文档只定**需求与技术选型**，不含实现细节代码。功能编号 `FR2-*`。
> 优先级：P0 必做 / P1 应做 / P2 可选 / P3 视情况。

---

## 1. 范围与批次

| 批次 | 主题 | 内容 | 优先级 |
| --- | --- | --- | --- |
| **S2.1** | 内容状态与排序 | 草稿 / 发布；置顶与排序权重 | P0 |
| **S2.2** | 媒体能力 | 图片上传、Markdown 图片回填、媒体库 | P0 |
| **S2.3** | 站点配置 | `site_profile` 云端化 + 后台可视化编辑 | P1 |
| **S2.4** | 运维能力 | 数据导入导出（JSON）、修改密码 | P1 |
| **S2.5** | 体验增强（可选） | 文章可分享直链、操作日志、回收站 | P2 / P3 |

**明确不做**：多管理员 / 角色权限、自建后端、富文本编辑器、评论系统、访问统计。

---

## 2. 功能需求

### 2.1 S2.1 内容状态与排序

| 编号 | 需求 | 验收要点 |
| --- | --- | --- |
| FR2-PUB-01 | 文章支持 **草稿 / 发布** 两种状态 | 后台可切换；草稿在展示站点**不可见** |
| FR2-PUB-02 | 列表展示状态、支持按状态筛选 | 状态徽标 + 筛选器 |
| FR2-PUB-03 | 仪表盘分别统计「已发布 / 草稿」 | 两个数字 |
| FR2-PUB-04 | 新增时可选「保存为草稿」或「直接发布」 | 表单底部两个按钮 |
| FR2-SORT-01 | 文章支持**置顶**与**排序权重** | 置顶文章在展示站点优先展示 |
| FR2-SORT-02 | 后台可调整顺序（数字输入或上移/下移） | 变更即时生效 |

### 2.2 S2.2 媒体能力

| 编号 | 需求 | 验收要点 |
| --- | --- | --- |
| FR2-MEDIA-01 | 后台上传图片，返回可公开访问 URL | 仅登录可用；类型/大小受限 |
| FR2-MEDIA-02 | Markdown 编辑器支持**插入图片**（按钮 / 拖拽 / 粘贴） | 自动插入 `![alt](url)` 到光标处 |
| FR2-MEDIA-03 | 媒体库：列表、复制链接、删除 | 需登录；删除前二次确认 |
| FR2-MEDIA-04 | 上传失败有明确提示（超限 / 类型不符 / 未登录） | 不静默失败 |
| FR2-MEDIA-05 | 展示站点图片正常加载（懒加载、窄屏不溢出） | 长图不撑破版式 |

### 2.3 S2.3 站点配置

| 编号 | 需求 | 验收要点 |
| --- | --- | --- |
| FR2-SITE-01 | 站点资料（姓名/英文名/定位/一句话/简介/GitHub/邮箱/技术栈/起始年份）存云端 | 新建 `site_profile` 单行表 |
| FR2-SITE-02 | 后台可视化编辑（页面可预览效果） | 表单校验：GitHub / 邮箱格式 |
| FR2-SITE-03 | 展示站点（首页 / 关于 / 页脚）改为读取云端配置 | 改完刷新即生效 |
| FR2-SITE-04 | 云端未配置时**回退默认值**，页面不得空白 | 回退到本地默认常量 |

### 2.4 S2.4 运维能力

| 编号 | 需求 | 验收要点 |
| --- | --- | --- |
| FR2-IO-01 | 一键导出全量数据（JSON：posts / projects / site_profile + 版本号 + 导出时间） | 生成文件可下载 |
| FR2-IO-02 | 导入 JSON：校验 → **差异预览**（新增/更新/冲突）→ 确认后写入 | 非法文件被拦截 |
| FR2-IO-03 | 导入前自动提示先导出备份 | 二次确认 |
| FR2-ACC-01 | 修改密码（需先验证当前密码） | 修改成功后其他会话失效 |

### 2.5 S2.5 体验增强（可选）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| FR2-ROUTE-01 | 展示站点改为独立路由（`/blog`、`/blog/:id` 可直达、可分享、可收录） | P2（需部署 rewrite） |
| FR2-LOG-01 | 操作日志（谁在何时改了什么） | P3 |
| FR2-TRASH-01 | 软删除 / 回收站（误删可恢复） | P3 |

---

## 3. 技术选型

> **总原则：不新增运行时依赖。** 全部能力用 Supabase 内置（Auth / DB / Storage）+ 现有库实现，保持产物体积与维护成本不变。

### 3.1 关键决策

| # | 需求 | 选型 | 理由 | 替代方案（不采用的原因） |
| --- | --- | --- | --- | --- |
| 1 | 草稿 / 发布 | `posts.status text not null default 'published'` + `check (status in ('draft','published'))` | 加法迁移、现有行默认已发布、零风险 | 布尔 `published`（扩展性差）；独立草稿表（数据分裂） |
| 2 | 展示区只看已发布 | 展示端查询加 `.eq('status','published')` | 过滤下沉到数据层，展示组件无需感知 | 前端过滤（草稿会被拉到浏览器，不安全且浪费流量） |
| 3 | 置顶 / 排序 | `posts.pinned boolean default false` + `posts.sort int default 0` | 布尔直观可切换；`sort` 处理同组精细排序 | 只用 `sort`（置顶需魔法值）；改用 `position` 浮点（易冲突） |
| 4 | 图片存储 | **Supabase Storage**，`assets` bucket（public 读） | 与现有 Supabase 同源、免后端、CDN 直出 | 第三方图床（不可控）；Base64 内嵌（体积爆炸）；自建服务（违背无后端约束） |
| 5 | 上传权限 | Bucket `file_size_limit ≈ 5MB` + `allowed_mime_types: image/*`；Storage 策略：anon `select`、authenticated `insert/delete` 且调用 `is_admin_session_valid()` | 与阶段一会话有效期策略保持一致，访客无法上传 | 仅 `authenticated`（无法约束旧会话）；公开写（危险） |
| 6 | 对象命名 | `assets/YYYY/MM/<uuid>.<ext>` | 避免同名覆盖、便于按时间归档 | 原名上传（重名覆盖）；单一目录（文件多后难管理） |
| 7 | 编辑器插图 | 原生 `DataTransfer`（拖拽）+ `paste` 事件，无第三方编辑器 | 复用现有 Textarea + 预览，不引入重型编辑器 | 引入 ProseMirror/CodeMirror（体积与改造成本高，且偏离 Markdown 约束） |
| 8 | 站点配置 | 单行表 `site_profile`（`id text primary key default 'default' check (id='default')`） | 单管理员单份配置，读写简单、并发无冲突 | key-value 配置表（过度设计）；继续硬编码（无法后台编辑） |
| 9 | 配置兜底 | 展示区读取失败/未配置时回退 `src/lib/site.ts` 默认值 | 保证线上永不自爆空白页 | 直接删除 `site.ts`（故障时页面空白） |
| 10 | 配置下发 | 新增 `SiteProvider`（Context）+ 并入现有 store 拉取 | 与 posts/projects 同一次请求、无额外往返 | 每个组件各自请求（重复请求、状态不同步） |
| 11 | 导入导出 | 纯前端：`JSON.stringify` + `Blob` 下载；导入用 `FileReader` + 手写校验 | 零依赖、数据不出浏览器；批量 `upsert` 一次请求 | 引入 ajv 做 schema 校验（为一个功能加依赖）；后端导入接口（无后端） |
| 12 | 导入写入 | PostgREST 数组批量 `upsert`，按 100 条分批 | 单请求完成、可控错误粒度 | 逐条写入（请求过多）；直接覆盖全表（危险） |
| 13 | 修改密码 | `supabase.auth.updateUser({ password })`，改前先 `signInWithPassword` 验证旧密码 | 使用官方 API；验证旧密码可防「离开未锁屏被人改密」 | 依赖邮件重置（单管理员场景体验差） |
| 14 | 文章直链（P2） | 展示区改用 `react-router` 独立路由 + 部署 rewrite | 可分享 / 刷新不丢 / 利于收录 | 保留页内切换（现状，无法分享文章） |
| 15 | 软删除（P3） | `deleted_at timestamptz?` + 展示与后台统一过滤 | 改动小、可恢复 | 独立回收站表（关联复杂） |

### 3.2 新增依赖

**无。** 若后续坚持不做文章直链（FR2-ROUTE-01），则本阶段完全不新增任何依赖。

### 3.3 数据库迁移脚本（Supabase → SQL Editor，幂等）

| 脚本 | 内容 |
| --- | --- |
| `0003_stage2_content_state.sql` | `posts` 增加 `status` / `pinned` / `sort` + 索引；写策略不变（仍要求会话有效） |
| `0004_stage2_site_profile.sql` | 新建 `site_profile` 表 + RLS（anon 只读、authenticated 可写且会话有效）+ 初始化默认行 |
| `0005_stage2_storage_assets.sql` | 创建 `assets` bucket（public / 5MB / `image/*`）+ Storage 策略 |

> 全部为**加法**变更；回滚只需 `drop column` / `drop table` / 删除 bucket。

---

## 4. 改动影响面

| 区域 | 涉及改动 |
| --- | --- |
| **后台** | 文章表单（状态、另存为草稿）、文章列表（状态徽标 / 筛选 / 置顶 / 排序）、仪表盘（草稿统计）、新增图片上传与媒体库、新增站点配置页、新增设置页（导入导出 / 改密码） |
| **展示端（只读）** | `store` 拉取加「仅已发布」过滤与排序；图片懒加载；站点配置改读云端（含默认值兜底） |
| **数据层** | 3 个迁移脚本；`Types` 增加可选字段 |
| **部署** | 仅当采用 FR2-ROUTE-01 时需要 rewrite 规则；其余无需改动 |

> **兼容性红线**：任何**语义变更**（`status` 过滤、`pinned` 排序）必须「展示端 + 后台」同一次提交一起改，禁止单边上线。

---

## 5. 验收标准

1. 草稿在后台可见、在展示站点不可见；切换为发布后展示站点立即可见；
2. 置顶文章在展示站点排在最前，取消置顶后恢复原顺序；
3. 后台可上传图片并把链接插入正文，展示页图片正常显示、窄屏不溢出；
4. 访客无法上传（Storage 策略拒绝），且旧会话（超 7 天）同样被拒；
5. 站点配置在后台修改后，展示站点首页 / 关于 / 页脚刷新即生效；云端异常时回退默认值不白屏；
6. 导出文件可再次导入并被正确识别；非法文件被拦截；
7. 修改密码后旧会话失效，需重新登录；
8. `typecheck` / `build` 通过；展示站点仍**零管理入口、零写逻辑**。

---

## 6. 风险与注意

| # | 风险 | 应对 |
| --- | --- | --- |
| 1 | 展示端漏加 `status` 过滤 → 草稿泄露 | 过滤下沉到数据层；验收时专门造一篇草稿验证 |
| 2 | Storage 策略只判角色、不判会话 | 策略内调用 `is_admin_session_valid()`，与阶段一一致 |
| 3 | 大图拖垮展示页 | Bucket 限 5MB + `image/*`；展示页图片 `loading="lazy"` |
| 4 | 导入覆盖线上数据 | 导入前强制导出备份 + 差异预览 + 二次确认 |
| 5 | 配置云端化后页面白屏 | 保留 `site.ts` 默认值兜底 |
| 6 | 文章直链改造影响部署 | 作为可选批次，单独排期并同步提供 rewrite 配置 |

---

## 7. 建议排期

| 顺序 | 内容 | 依赖 |
| --- | --- | --- |
| 1 | S2.1 内容状态与排序（`0003` + 展示端过滤） | 无 |
| 2 | S2.2 图片上传与媒体库（`0005`） | 无 |
| 3 | S2.3 站点配置（`0004`） | 无 |
| 4 | S2.4 导入导出 + 修改密码 | S2.1 之后（导出需含 `status`） |
| 5 | S2.5 可选增强 | 视需要 |

---

*（本文档为草案，确认后按批次实施，并在 `ADMIN-REQUIREMENTS.md` 中同步更新版本与实现状态。）*
