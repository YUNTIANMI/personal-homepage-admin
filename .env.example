# ===== 后端 API 地址 =====
# 前端所有数据读写、认证、媒体上传都走自研 Spring Boot 后端的 /api/* 接口。
# 默认指向本地 8080；部署时改为后端实际地址。
#
# 配置方法：复制本文件为 .env.local，按需修改 VITE_API_BASE，然后重启 npm run dev
# 后端接口契约见 docs/BACKEND-ARCHITECTURE.md
VITE_API_BASE=http://localhost:8080/api
