# ===== 云端数据存储（可选） =====
# 填入 Supabase 项目的地址与匿名密钥后，文章 / 项目数据将保存到云端（换电脑、换浏览器都能看到）。
# 留空则退回「浏览器本地存储」模式，功能照常可用，只是数据不跨设备。
#
# 获取方式：https://supabase.com/dashboard → 创建项目 → Settings → API
#   Project URL          → 填到 VITE_SUPABASE_URL
#   anon public API Key  → 填到 VITE_SUPABASE_ANON_KEY
# 配置方法：复制本文件为 .env.local，填入上面两项，然后重启 npm run dev
# 详细步骤（建表、权限策略）见 README「云端存储」章节
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
